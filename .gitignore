/vendor
/.idea
/.vscode
/.vagrant
.phpunit.result.cache
.opencode/ralph-loop.local.md

# Local runtime state — event log, settings. Never commit: it accumulates
# prompts, file contents and tool output as soon as the agent is used.
.paider/
/build

# Local dev artifacts — planning scratch, score rounds, bench harness, TUI captures
PLAN-PHASES-1-10.md
PRISM-TAKEAWAYS.md
RELEASE-V1-*.md
ROADMAP-*.md
overnight-v1-report.html
blog/
# m1/ dev harness stays local — EXCEPT the fixture, which E2ETraceTest and
# TokenKillerTest read as their corpus; CI needs it (broke 2026-08-17 when all
# of m1/ was untracked)
m1/*
!m1/fixture/
!m1/bench/
# preflight is a dependency of the tracked bench (and of the suite via
# ForeignRepoBenchLiveGateTest) — tracked code must not depend on untracked files
!m1/preflight.sh
design/*
!design/captures/
design/captures/*
!design/captures/paider-tui.png
council_meetings/
m1/bench/paider-100.md
